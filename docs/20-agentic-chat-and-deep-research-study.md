# Papertrend Agentic Chat And Deep Research Study

Date: 2026-08-06

## Scope And Method

This study inventories every unique repository supplied for this work and performs a source-level architecture review of the code paths that determine behavior: orchestration, retrieval, memory, prompting, grounding, citations, chart recommendation, security, evaluation, and deployment. Duplicate entries for STORM and Local Deep Research are reviewed once.

The combined repositories contain more than 100,000 tracked files. "Every file" therefore includes generated API types, snapshots, translations, fixtures, bundled assets, lockfiles, vendored dependencies, images, and documentation copies. Those files were inventoried with `git ls-tree`, but they were not treated as independent algorithmic evidence. The semantic review follows executable call paths and their tests. This is both more reproducible and more honest than claiming that every generated byte was read for meaning.

The leaked-system-prompt collection was treated as a pattern catalog only. Papertrend does not copy proprietary prompts. Its prompt contract is original, auditable, versioned, and specific to authorized research repositories.

## Repository Findings

| Repository | Useful pattern | Papertrend decision |
| --- | --- | --- |
| system_prompts_leaks | Explicit role, tool, safety, formatting, and failure contracts | Adopt the structure, not leaked text |
| chatgpt-custom-knowledge-chatbot | Simple conversational RAG baseline | Keep only as a baseline comparison |
| Pathway llm-app | Live indexing, incremental updates, hybrid indexes, cache | Adopt event-driven repository memory updates |
| Flowise | Graph/state/tool/loop orchestration, checkpoints, HITL | Keep typed graph steps and observable tool traces |
| WeKnora | ReAct, scoped sources, Wiki memory, citations drawer, RBAC, audit | Adopt scoped retrieval, repository digests, source UX, and hard isolation |
| ai-pdf-chatbot-langchain | Streaming PDF RAG reference flow | Already exceeded by Papertrend's structured extraction |
| gpt4free | Provider adapters and compatible API surface | Reject as a production provider dependency due to reliability, credential, and terms risk |
| nanobot | Small readable agent core, tool routing, memory, delegation | Prefer a compact typed core over agent proliferation |
| Chatbox | Multi-provider conversation UX and local-first configuration | Adopt clear model/runtime UX only where useful |
| Quivr | Knowledge containers and workflow-oriented RAG | Papertrend projects/folders remain the knowledge boundary |
| AstrBot | Context compression, skills, MCP, sandboxing | Adopt compression; require a sandbox before any code tool |
| Langchain-Chatchat | Offline/private RAG and replaceable model/vector layers | Preserve provider abstraction and lexical fallback |
| RAGFlow | Deep document parsing, hybrid retrieval, citations, agentic workflows | Adopt layout-aware evidence and retrieval observability incrementally |
| LightRAG | Entity/relationship graph plus local/global retrieval | Evaluate graph retrieval for cross-paper concept synthesis, not basic QA |
| STORM / Co-STORM | Perspective-guided questions, outline-before-writing, modular research/writing | Adopt perspective planning and evidence-first outlines for deep research |
| RAG_Techniques | Query rewriting, HyDE, fusion, reranking, compression, corrective/self RAG, evaluation | Select techniques by measured eval gains, not feature count |
| Haystack | Typed components, routers, loops, async pipelines, evaluation | Matches Papertrend's typed capability and fallback direction |
| Memvid | Portable indexed memory artifact | Not needed while Cloud SQL is the authoritative multi-user store |
| llmware | Library abstraction, hybrid/metadata query, batched source prompts, evidence checking | Adopt batching and post-answer evidence checks |
| txtai | Semantic workflows, graphs, SQL-style search | Useful reference; pgvector avoids another database |
| LEANN | Storage-efficient recomputation-based vector index | Revisit only when vector storage becomes a measured cost problem |
| Microsoft AI Agents for Beginners | Design patterns for tools, planning, multi-agent, memory, metacognition, context engineering | Use as maintainable pattern guidance and training material |
| Ruflo | Harness, memory, budgets, telemetry, security and coordinated tools | Adopt budgets/telemetry; avoid unnecessary swarms |
| Local Deep Research | Iterative search, configurable local/private sources, report synthesis | Adopt bounded iterative exploration and source independence |
| DeerFlow | Planner-led research, subagents, skills, memory, sandbox, long-running workflows | Strong model for durable deep-research jobs and progress traces |
| last30days-skill | Time-bounded source discovery and explicit recency | Add recency constraints only when the user asks for current context |
| Alibaba DeepResearch | Trained tool-using research behavior and benchmark evaluation | Adopt evaluation discipline, not model-specific thought tags |
| DeepSearcher | Private-data-first retrieval, query decomposition, reflection, pluggable loaders/embeddings | Closest match to Papertrend's internal-first hybrid research direction |
| Flint Chart | Semantic-role and cardinality-based chart recommendation followed by renderability validation | Adopt a compact data-aware chart guard after LLM planning |
| Grafana | Mature visualization plugins, field configuration, transformations, and suggestion UX | Adopt validation and progressive disclosure, not its dashboard runtime |
| Data Formulator | LLM chart intent plus typed data transformations and semantic type resolution | Keep LLM intent separate from deterministic data execution |
| chart-agent | Minimal LLM-to-chart prototype | Useful baseline only; insufficient grounding and validation for research data |
| DeepDiagram | Tool-dispatched ECharts generation and iterative editing | Adopt editable chart rationale later; reject synthetic-data instructions |
| D3 | Low-level scales, layouts, axes, interactions, and rendering primitives | Keep Recharts for now; D3 does not solve agent planning or grounding |

## Source Audit Ledger

The following are the primary executable audit anchors. Related imports, schemas, and tests were followed where they affect the observed behavior.

| Repository | Source-level anchors | Finding / decision |
| --- | --- | --- |
| chatgpt-custom-knowledge-chatbot | `bots/simple_vector_index.py`, `bots/chat_completion.py`, `main.py` | Basic vector index and prompt-only grounding; retained only as a failure-prone baseline |
| Pathway llm-app | `templates/adaptive_rag/app.py`, template YAML pipelines | Persistence and incremental UDF caching support event-driven repository memory |
| Flowise | `packages/components/src/agentflowv2Generator.ts`, server generator services, component nodes | Graph tooling is flexible but much broader than Papertrend needs; keep typed steps without importing the runtime |
| WeKnora | `internal/agent/token/compress.go`, knowledge-search/rerank tests, retriever repositories | Preserve current turn and tool pairs during compression; enforce scope below the model |
| ai-pdf-chatbot-langchain | `backend/src/retrieval_graph/graph.ts`, prompts/state/retrieval helpers | Structured route-to-retrieve/direct is sound but a binary router is too narrow for compound Papertrend tasks |
| gpt4free | provider adapters under `g4f/Provider`, client factory/service | Provider compatibility is useful; unofficial provider behavior is rejected for production |
| nanobot | `nanobot/agent/context_governance.py`, `loop.py`, `autocompact.py`, tests | Small core, bounded loops, and context governance are preferable to agent proliferation |
| Chatbox | `src/main/session-attachment-rag/*` | Background indexing, explicit attachment states, transactional replacement, and cleanup are useful memory lifecycle patterns |
| Quivr | `core/quivr_core/rag/quivr_rag_langgraph.py`, entities/prompts | Decompose a request into multiple tasks and track completeness per task; directly motivated multi-capability planning |
| AstrBot | `context/compressor.py`, `tool_loop_agent_runner.py`, retrieval fusion | Preserve recent exact turns, summarize old turns, cap loops, detect repeated tools, and spill oversized results |
| Langchain-Chatchat | `search_local_knowledgebase.py`, knowledge-base search services | Knowledge base is a typed tool with constrained scope and thresholds |
| RAGFlow | `agent/component/agent_with_tools.py`, retrieval components and prompt utilities | Tool schemas, bounded rounds, message fitting, citations, and structured-output repair are valuable; full platform adoption is unnecessary |
| LightRAG | `lightrag/operate.py`, graph/query storage modules | Progress events and local/global relationship retrieval suit later cross-paper synthesis; GraphRAG is not required for ordinary QA |
| STORM | retriever and article-generation modules | Perspective planning and outline-first research improve long reports, not exact repository operations |
| RAG_Techniques | runnable adaptive retrieval, contextual compression, CRAG, fusion, HyDE examples | Techniques must earn adoption through retrieval and faithfulness evaluation; no universal HyDE |
| Haystack | `haystack/components/agents/agent.py`, retrievers, faithfulness/context evaluators | Typed components, loops, and explicit evaluators validate Papertrend's bounded tool-and-audit design |
| Memvid | `src/memvid/planner.rs`, search/graph modules | Token/page-aware segmentation is useful; portable memory storage is unnecessary beside Cloud SQL |
| llmware | `llmware/retrieval.py`, `agents.py`, `embeddings.py` | Query modes, metadata-preserving evidence, and batched source prompts support hybrid retrieval |
| txtai | `src/python/txtai/agent/base.py`, agent process/tool modules | Agents fit multi-faceted requests; deterministic workflows remain better for exact operations |
| LEANN | `packages/leann-core/src/leann/api.py`, searcher/chunking/embedding modules | Storage-efficient vector recomputation is deferred until pgvector storage is a measured bottleneck |
| Microsoft AI Agents for Beginners | planning, multi-agent, metacognition, protocol code samples | Design guidance supports bounded planning and tool contracts; not a production dependency |
| Ruflo | orchestrator, memory, routing, telemetry packages and tests | Adopt budgets and observability; reject a swarm as default chat architecture |
| Local Deep Research | `parallel_search.py`, evidence evaluator, progressive/follow-up strategies | Parallel queries require isolated context; evidence records and bounded iteration fit deep research |
| DeerFlow | lead agent, context middleware, loop detection, compaction, reflection | Durable context, loop detection, and long-job state are strong deep-research patterns |
| last30days-skill | planner/query/rerank pipeline | LLM-first planning plus deterministic validation; source routing should be domain-specific, not a universal intent list |
| Alibaba DeepResearch | ParallelMuse aggregation, NestBrowse/WebSailor tool agents | Compress independent trajectories into evidence reports, then converge; do not expose or depend on hidden reasoning text |
| DeepSearcher | `agent/deep_search.py`, collection router, RAG agents | Decompose, retrieve, reflect on gaps, issue new queries, and stop at a bounded iteration count |
| Flint Chart | `core/chart-type-recommendation.ts`, Chart.js recommendation/instantiation | Recommend from semantic roles/cardinality, then reject encodings that cannot render honestly |
| Grafana | visualization suggestion components, panel plugin contracts, transformations | Separate visualization selection, field configuration, and rendering; full Grafana embedding is excessive |
| Data Formulator | `workflows/chart_semantics.py`, chart agents | Infer semantic types and compile an LLM request into typed transforms rather than executable arbitrary code |
| chart-agent | `server/app.py`, `server/helper_functions.py`, React client | Demonstrates the baseline LLM-code-generation path; rejected because it lacks trustworthy data contracts |
| DeepDiagram | `backend/app/agents/charts.py`, dispatcher/flow agents | Keep chart history/rationale concept; reject instructions to fabricate realistic data |
| D3 | `src/index.js`, API modules and visualization primitives | Excellent renderer primitives, but no retrieval, planner, or factuality layer; no migration from Recharts now |

## Main Conclusion

A system prompt is necessary but not sufficient. Production quality comes from enforcing the same contract across an execution pipeline:

1. Resolve conversation language and authorized scope.
2. Let an LLM director select the smallest sufficient ordered set of typed capabilities, with schema validation and a scope-preserving fallback.
3. Use deterministic tools for exact operations and hybrid retrieval for semantic evidence.
4. Rerank by relevance, evidence strength, diversity, and coverage.
5. Expand retrieval when evidence is insufficient.
6. Synthesize from compressed evidence, not raw repository dumps.
7. Audit claims and citations against the evidence ledger.
8. Return useful partial work with explicit limitations when a provider fails.
9. Record prompt version, tools, coverage, latency, token cost, and eval results.

## Implemented

- Added a versioned Papertrend prompt constitution in `src/lib/papertrend-system-prompt.ts`.
- Added task overlays for request direction, evidence reranking, grounded answers, faithfulness audits, corpus mapping, corpus synthesis, and chart planning.
- Applied the contract to both Repository Chat V1/V2 planning, reranking, answer synthesis, citation repair, exhaustive map-reduce, and chart tool selection.
- Added tests for scope completeness, prompt-injection boundaries, owner isolation, title-first answers, language continuity, citations, and task composition.
- Replaced the one-operation planner bottleneck with a bounded one-to-four capability plan, so compound requests can count, analyze, list, and visualize without silently dropping part of the request.
- Added a focused-evidence sufficiency check and one bounded expansion round. It identifies missing evidence needs and issues narrower retrieval queries before synthesis.
- Added retrieval diagnostics for rounds, sufficiency checks, and unresolved evidence needs.
- Added a data-aware chart recommendation guard derived from Flint Chart/Data Formulator patterns. It preserves LLM analytical intent while correcting unreadable pie charts and inappropriate temporal encodings.
- Added deterministic tests for compound planning and chart semantic validation.

## Why Task Contracts Do Not Limit User Capability

The contract is not an intent whitelist. A user may ask an arbitrary compound question. The LLM director translates that question into one to four composable capabilities, and each capability has typed, owner-scoped tool arguments. The bounded count limits runaway tool loops and cost, not prompt variety. If a request requires more work, one capability can itself run a complete map-reduce or asynchronous report over the entire repository.

The previous limitation was architectural: `RepositoryExecutionPlan` allowed exactly one operation. That could discard part of a request such as "count this phrase, compare the papers, and chart the result." The plan now preserves an ordered capability set and merges grounded results, citations, charts, coverage, limitations, and diagnostics.

## Deliberate Non-Changes

- No copied Claude, Gemini, ChatGPT, or other leaked vendor prompt.
- No migration to a second vector database; Cloud SQL and pgvector remain sufficient.
- No arbitrary code execution tool without a sandbox and explicit policy.
- No multi-agent swarm for ordinary chat. Specialized agents are justified only for deep research jobs where parallel evidence collection has measurable value.
- No universal HyDE step. Hypothetical answers can improve recall but can also bias scientific retrieval; it should be evaluated and enabled selectively.

## Next Evaluation Gate

Measure planner accuracy, exhaustive-scope coverage, retrieval recall, citation validity, claim faithfulness, Thai/English continuity, latency, and cost on a fixed golden set. New RAG techniques should ship only when they improve those scores without weakening owner/project/folder isolation.

## Knowledge Chat V3 Recovery

The pilot regression where Papertrend answered as a generic local-files assistant came from route bypass, not missing retrieval infrastructure. Web Search, missing project context, an unhandled planner result, or a repository exception could all enter a generic prompt that denied repository access.

Knowledge Chat V3 changes that contract:

1. Resolve an owner-authorized scope before planning. Precedence is selected papers, folder, explicit project, then all owner projects.
2. Send every authenticated normal-chat request through the knowledge orchestrator. Web retrieval supplements repository evidence instead of disabling it.
3. Use `converse` only for genuinely unrelated conversation. It remains Papertrend-aware and never claims the product cannot access an authorized repository.
4. Persist an authoritative scope snapshot with every user message so historical scope labels remain stable.
5. Fail closed with a request ID and scoped limitation. A repository exception never falls through to an ungrounded generic answer.
6. Log scope kind, operation, latency, retrieval rounds, evidence count, and failure stage without logging prompts or paper text.

`KNOWLEDGE_CHAT_V3_ENABLED=false` restores the prior route during pilot rollout. Chat V2 planning and retrieval remain available underneath the new route contract.

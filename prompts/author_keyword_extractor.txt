You extract only author-provided keywords from research papers.

Rules:
- Extract keywords only when there is an explicit labeled keyword list from the paper, such as "Keywords:", "Key words:", "Author keywords:", "Index terms:", or an equivalent label.
- Do not infer keywords from the abstract, title, body, headings, or bibliography.
- If there is no explicit labeled author keyword list, return has_author_keywords=false and an empty keywords list.
- Keep each keyword as a concise phrase. Remove bullets, numbering, trailing punctuation, and duplicated labels.
- Preserve meaningful technical wording from the paper. Translate only when the labeled list itself is already translated in the supplied context.
- The evidence field must quote the labeled keyword-list text that supports the extraction.

Candidate labeled keyword text:
{candidate_text}

Paper context for disambiguation:
Title: {title}
Abstract/claims:
{abstract_claims}
